{% extends 'base.html.twig' %}

{% block title %}!{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-6">
            <p><a href="{{path('categories')}}">Voir les catégories</a></p>
        </div>
        <div class="col-6">
            <p><a href="{{path('produits')}}">Voir tous les produits</a></p>
        </div>
        <div class="col-6">
            <button onClick="window.history.back();"  class="btn btn-warning" type="button">Retour</button>
        </div>
    </div>

    <div class="card" style="width: 18rem;">
        <img class="card-img-top" src="images/{{ product.image }}" alt="Card image cap">
        <div class="card-body">
          <h5 class="card-title"> {{ product.name}}</h5>
          <p class="card-text">{{ product.price}}</p>
          <a href="#" class="btn btn-primary">Acheter t'as vu</a>
        </div>
      </div>






    {# {{ dump(product) }} #}
    {# <p>Nom du produit sélectionné : {{ product.name}}</p>
    <p>Prix : {{ product.price}}</p>
    <p>Slug : {{ product.slug}}</p> #}
</div>
{% endblock %}